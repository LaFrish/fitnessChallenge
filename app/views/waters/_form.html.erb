<%= form_for [@user, @water], :html => { :multipart => true,  :class => "pictureInput" } do |f| %>
  <% if water.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(water.errors.count, "error") %> prohibited this water from being saved:</h2>

      <ul>
      <% water.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


        <%= f.hidden_field :user_id, :value => @user.id %>
    <div class="field">
      <%= f.label :waterDrank %>
      <%= f.text_field :waterDrank %>
    </div>

    <div class="field">
      <%= f.label :point %>
      <%= f.text_field :point %>
    </div>

    <div class="field">
      <%= f.label :waterPoint %>
      <%= f.text_field :waterPoint %>
    </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
