<p id="notice"><%= notice %></p>

<h1>Waters</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.waters.each do |water| %>
      <tr>
        <td></td>
        <td><%= link_to 'Show', user_water_path(@user, @water) %></td>
        <td><#%= link_to 'Edit', edit user_waters_path(@user, @water) %></td>
        <td><%= link_to 'Destroy', [@user, @water], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<#%= link_to 'New Water', new_water_path %>
